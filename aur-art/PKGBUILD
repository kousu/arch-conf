pkgname=kousu-aur-art
pkgdesc="Configuration management for kousu's brain goblins (AUR packages)"
arch=('any')
url="https://github.com/kousu/arch-conf"

pkgver=0
pkgrel=1
pkgver() {
  [ -r "$startdir"/.pkgver ] && cat "$startdir"/.pkgver || (echo "r$(git rev-list --count HEAD).commit=$(git describe --always --dirty | sed s/-/+/g)")
}

depends=(
  kousu-aur-base

  # graphics
  pinta   # the BEST modern paint program

  # audio
  ninjas2-git
)
