pkgname=kousu-aur-art
pkgdesc="Configuration management for kousu's brain goblins (AUR packages)"

# load common settings; be aware if you ever split up this repo.
[ -f ../lib/lib.PKGBUILD ] && cp -lf ../lib/lib.PKGBUILD .lib.PKGBUILD
. .lib.PKGBUILD || exit 1

depends=(
  kousu-aur-base

  # graphics
  pinta-git   # the BEST modern paint program

  # audio
  ninjas2-git

  lingot  # instrument tuner

  # video
  #davinci-resolve
)
