pkgname=kousu-aur-work
pkgdesc="Configuration management for kousu's job life (AUR packages)"

# load common settings; be aware if you ever split up this repo.
[ -f ../lib/lib.PKGBUILD ] && cp -lf ../lib/lib.PKGBUILD .lib.PKGBUILD
. .lib.PKGBUILD || exit 1

depends=(
  kousu-work
  kousu-aur-base

  # comms
  zoom

  # productivity
  # https://codeberg.org/unfa/HyperTimer
  hypertimer-bin

  # typesetting
  typstyle  # for Typst.app

  # programming
  matlab-support
)
