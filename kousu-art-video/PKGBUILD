pkgname=kousu-art-video
pkgdesc="Configuration management for kousu's creative video editing"

# load common settings; be aware if you ever split up this repo.
[ -f ../lib/lib.PKGBUILD ] && cp -lf ../lib/lib.PKGBUILD .lib.PKGBUILD
. .lib.PKGBUILD || exit 1

depends=(
  kousu-desktop

  # hardware
  # alsa-card-profiles # is this needed? it's for making fancy professional soundcards work, I think

  # video/music
  yt-dlp
  pitivi     # video editor (Gnome)
  #kdenlive  # video editor (KDE)
  #flowblade
  #lightworks # AUR
  #davinci-resolve # AUR

  #lives # VJ tool # AUR
  #shotcut

  video-trimmer
  #vidcutter
)
