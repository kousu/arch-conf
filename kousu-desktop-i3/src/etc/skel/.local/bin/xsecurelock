#!/bin/sh

# TODO: figure out how to move these envs into
#       ~/.config (maybe ~/.config/sx/sxrc ?)
export XSECURELOCK_SAVER=saver_mpv
export XSECURELOCK_PASSWORD_PROMPT=kaomoji
export XSECURELOCK_AUTH_BACKGROUND_COLOR=pink
export XSECURELOCK_AUTH_FOREGROUND_COLOR=black
/usr/bin/xsecurelock "$@"
