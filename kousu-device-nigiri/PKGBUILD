pkgname=kousu-device-nigiri
pkgdesc="Configuration management for kousu's \"nigiri\" thumbdrive system"

# load common settings; be aware if you ever split up this repo.
[ -f ../lib/lib.PKGBUILD ] && cp -lf ../lib/lib.PKGBUILD .lib.PKGBUILD
. .lib.PKGBUILD || exit 1

depends=(
  kousu-bootloader-efi
  kousu-desktop-kde
  kousu-laptop-mode

  linux-firmware-intel
  broadcom-wl # if the thumbdrive were booted on a system with only broadcom wifi we would be stuck without these drivers
)

conflicts=(
  kousu-device-allium
  kousu-device-bomp
)
